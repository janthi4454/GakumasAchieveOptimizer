{
  "name": "gakumasu-achievement",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "run-p \"build-only {@}\" --",
    "build-only": "vite build"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vite": "^8.0.10",
    "npm-run-all2": "^8.0.4"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
